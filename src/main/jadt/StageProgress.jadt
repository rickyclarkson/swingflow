package com.github.rickyclarkson.swingflow

StageProgress =
  Success(String brief, Option<String> detailed)
 |InProgress(Seconds remaining, String brief, Option<String> detailed)
 |Failed(String brief, Option<String> detailed)

Seconds = Seconds(int seconds)

Option<T> =
  Some(T t)
 |None